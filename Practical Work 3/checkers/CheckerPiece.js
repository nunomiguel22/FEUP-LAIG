
class CheckerPiece {

    constructor(scene, model, type) {
        super(scene, model);

        this.mat = new CGFappearance

        this.type = type;
        this.model = model;
    }
}
